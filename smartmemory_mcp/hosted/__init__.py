"""Hosted (OAuth-protected, multi-tenant) MCP server support — PLAT-MCP-HOSTED-1.

Everything in this package is inert unless the server is started in hosted mode
(`--hosted` / `SMARTMEMORY_MCP_MODE=hosted`). Local/stdio behaviour is unchanged.
"""
